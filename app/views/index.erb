<!DOCTYPE html>
<html>
    <head>
        <title>Restful Routes Basic Lab</title>
    </head>

    <body>
        <ul>
            <% Recipe.all.each do |recipe| %>
            <li> <a href="/recipes/<%= recipe.id%>"><%= recipe.name%></a></li>
            <% end %>
        </ul>
    </body>
</html>
